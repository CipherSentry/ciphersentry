{
  "name": "@ciphersentry/verifier-daemon",
  "version": "0.2.0-alpha.1",
  "private": true,
  "type": "module",
  "description": "Verifier daemon alpha — deterministic WASM re-execution for CipherSentry tasks",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "daemon": "node --experimental-transform-types src/daemon.ts",
    "test": "node --experimental-transform-types --test test/*.test.ts",
    "build": "tsc -p ."
  },
  "engines": {
    "node": ">=22"
  }
}
