{
  "name": "@machinarc/indexer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Machinarc indexer — Postgres state transitions + ClickHouse receipt graph",
  "scripts": {
    "indexer": "node --experimental-strip-types src/server.ts",
    "schema:ch": "node --experimental-strip-types src/db.ts --apply-ch-schema"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "pg": "^8.13.0"
  }
}
